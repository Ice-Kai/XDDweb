# 薛大大设计网（xuedda-next）— 换号接手交接（2026-06-25）

> 写给下一个接手的 Claude。读完这份 + `HANDOFF.md`（第 8 节 CHANGELOG 逐条改动、第 9 节安全审计）即可接手，不用翻聊天记录。
> 项目：`D:\aliyun-site\xuedda-next`（主项目，Astro）；`D:\aliyun-site\www.xuedda.com`（旧 PHP 站，只读参考）。

## 0. 接手第一步（按顺序）
1. 本文件（当前状态 + 红线 + 待办）
2. `D:\aliyun-site\HANDOFF.md` 第 8 节 **2026-06-25** 那条（本轮全部改动）+ 第 9 节安全结论
3. 记忆 `C:\Users\Administrator\.claude\projects\D--aliyun-site\memory\MEMORY.md`（尤其用户表共用决策）
4. **硬性要求：每次改动都追加到 `HANDOFF.md` 第 8 节 CHANGELOG。**

## 1. 一句话背景
旧站 `xuedda.com`（ThinkPHP5+MySQL 设计资源下载站）正用 **Astro 6 SSR + React/原生JS + mysql2** 重建。当前：新旧并行测试，新站先上线试跑。

## 2. 怎么跑 & 验证
- **Astro 6 SSR** + Tailwind v4（多为手写 CSS）+ mysql2，Node v24。env 走 `import.meta.env`（**不是** Next，`NEXT_PUBLIC_` 无效）。
- **Docker MySQL** 容器 `xuedda-mysql`：`127.0.0.1:3306`，库 `xuedda`(新)+`legacy`(旧全量 dump)。没起就 `docker compose up -d`。
- 开发：`npm run dev`（4321，HMR）。构建：`npm run build`（**慢 3-4min**）。
- **验证技巧**：改完用 `curl` 取 SSR HTML/脚本模块 + grep 校验，别每次 build。测 API 要带浏览器 UA（middleware 拦 curl UA → 误导的 "Access Denied"）。后台脚本是内联/打包模块，取 `…/[...path].astro?astro&type=script&index=0&lang.ts` 校验（curl 带 `[` 要 `-g`）。

## 3. 🔴 红线（不可违背）
1. **数据安全最高优先**：`legacy.*` 是 11.8 万真实用户的真表。动它前先 SELECT 确认；写只单行 `WHERE id=? LIMIT 1`，绝不批量。用户积分/会员是**真写会员表**，测试务必用测试号。
2. **用户表共用 `legacy.lz_member`**：登录/注册/VIP/积分全读写它（不是 xuedda.members）。会员密码 `md5(md5(pwd))`，管理员 `md5(pwd)`（单重，不同！）。代码 `src/lib/member.ts`、`api/admin/login.ts`。
3. **测试站不暴露真实登录数据**（2026-06-23 决策）：部署到 belongstoai.com 测试时，部署实例的 member 表要换成空/测试表——**此步未做**，接手首件事和用户敲定。

## 4. 后台现状（本轮重点，`src/pages/admin/[...path].astro` 单文件）
⚠️ 单文件巨型内联页：HTML+CSS+原生JS 全在一起，JS 靠 class/id 绑定。**改样式安全，改结构小心**。
已有 6 个侧栏模块：
- **01 工作台** / **02 栏目管理**（栏目树表格 CRUD）/ **03 资源管理** / **04 反馈管理** / **05 用户管理**(新) / **06 操作日志**(新)
- **资源管理**：左=可折叠+**彩色分组**栏目树（每主栏目一色、子栏目继承色带、点末级叶子才筛选）；中=Excel 行列表（无封面图、不显网盘链接、加大行+翻页）；右=**抽屉式上传表单**（点新增/编辑/批量才滑出）。
- **批量上传** = 图+链接配对：选多图/选文件夹 → 每图填网盘分享文本（自动拆链接/提取码）→ 客户端压缩 ≤300KB → 成对导入。
- **用户管理**：搜/列/翻页 + 抽屉改积分/会员/到期 + 看下载记录 + 登录时间。后端 `api/admin/users.ts`、`api/admin/users/[id].ts`。
- **操作日志**：`lib/adminlog.ts`(懒建 `xuedda.admin_log`) + `api/admin/logs.ts`，已插桩资源 create/update/hide。
- **链接扫描**：`api/admin/scan-links.ts` + 资源列表「扫描本页链接」按钮（**百度反爬，尽力而为，多为"未知"需人工核**）。

## 5. 待办（接手继续，用户已点单）
1. **操作日志补全**：批量操作 + 用户改积分 也记日志（现仅资源 create/update/hide）。
2. **自动序号**：按时间自动编序号 + 拖动调显示位置（part3 剩余，未做）。
3. **新站下载落库**：`api/download/[id].ts` 写下载日志（现下载记录只有 `legacy.lz_download_log` 历史数据，不含新站行为）。
4. **`c/[slug].astro` 局部刷新**：灯光/软件/视频/其他分类页同款（model.astro 已做，这些未做）。
5. **AI生图后端接 key**：`ai-image.astro`+`api/ai-image/generate.ts` 已就绪，填 `.env` 的 `IMAGE_PROXY_VERCEL_URL` 或 `IMAGE_PROXY_BASE_URL`+`IMAGE_PROXY_API_KEY` 即通，无需改码。注意这是站内本地页，与外链 `ai.belongstoai.com`(`settings.ai_image_url`)两套，外链优先。
6. **并行/部署遗留**（见 06-23 文档）：旧站同机并行、测试站换空 member 表、封面迁 OSS、大陆 Claude 不通→国产视觉模型。

## 6. 关键文件地图
- 后台：`src/pages/admin/[...path].astro`（单文件）。后台 API：`src/pages/api/admin/*`（content(s)/categories/users/logs/scan-links/feedback/login，均受 `middleware.ts` 鉴权，非 GET 还过 CSRF）。
- 鉴权：`src/lib/member.ts`(会员→legacy.lz_member)、`src/lib/auth.ts`(管理员 md5、token)、`src/middleware.ts`、`src/lib/ratelimit.ts`。
- 日志：`src/lib/adminlog.ts`。数据：`src/lib/content.ts`（资源查询/搜索）、`src/lib/db.ts`。
- 前台：`src/pages/index.astro`、`c/[slug].astro`、`c/model.astro`(已加局部刷新)、`d/[id].astro`、`ai-image.astro`(本地生图)、`ask.astro`/`search.astro`、`member/*`。外壳 `src/layouts/Base.astro`，样式 `src/styles/global.css`。

## 7. 用户画像
中文沟通。重数据安全、重"闭环不留死按钮"、重成本（AI 走国产省 token）。喜欢先讲清技术权衡再动手；对 UI 辨识度/细节要求高（后台 UI 反复打磨过）。
